# Carabiner bridge runtime dependencies
#
# Pinned 2026-07-09 by main Hermes session after verifying against the
# actual install at /usr/local/lib/hermes-agent (v0.18.2, commit fac85518).
# The Fable audit said hermes-agent==0.14.0 / mcp==1.9.0; this machine
# has hermes-agent==0.18.2 and mcp-1.26.0 vendored. Use what the build
# host actually has and record the pin here.

fastapi>=0.110
uvicorn[standard]>=0.27
python-socketio>=5.11
httpx>=0.27
mcp==1.26.0
itsdangerous>=2.1