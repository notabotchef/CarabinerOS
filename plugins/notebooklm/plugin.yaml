name: notebooklm
title: NotebookLM (Remote Auth)
description: >
  Query Google NotebookLM notebooks via the notebooklm-mcp server.
  Includes a remote-browser authentication flow so users can log into
  Google from their host browser while everything runs inside the A0
  container.
version: 0.1.0
settings_sections:
  - external
per_project_config: false
per_agent_config: false
